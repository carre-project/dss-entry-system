#!/bin/bash
echo "Deploy to Docker PRODUCTION server..."
cd dist
npm install
git init
git remote add deploy dokku@athina.med.duth.gr:carre-entry-system
git add .
git commit -am"deploy"
git push -f deploy master
cd ..